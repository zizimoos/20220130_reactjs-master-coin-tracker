const Price = () => {
  return (
    <>
      <div>FOUNDERS</div>
      {/* <div>
        {coinData?.team.map((item, index) => (
          <li key={index}>
            {item.name} - {item.position}
          </li>
        ))}
      </div>
      <div>WHITE PATER</div>
      <p>{coinData?.whitepaper.link}</p>
      <img src={coinData?.whitepaper.thumbnail} alt="whitepaper" />
      <div>LINKS EXTENDED</div>
      <div>
        {coinData?.links_extended.map((item, index) => (
          <li key={index}>{item.url}</li>
        ))}
      </div>
      <div>LINKS</div>

      <div>
        {coinData?.links.facebook ? (
          <a href={coinData?.links.facebook}>facebook</a>
        ) : null}
      </div>
      <div>
        {coinData?.links.reddit && <a href={coinData?.links.reddit}>reddit</a>}
      </div>
      <div>
        {coinData?.links.website && (
          <a href={coinData?.links.website}>website</a>
        )}
      </div>
      <div>
        {coinData?.links.website && (
          <a href={coinData?.links.website}>website</a>
        )}
      </div>
      <div>
        {coinData?.links.youtube && (
          <a href={coinData?.links.youtube}>youtube</a>
        )}
      </div>
      <div>
        {coinData?.links.source_code && (
          <a href={coinData?.links.source_code}>source_code</a>
        )}
      </div> */}
    </>
  );
};
export default Price;
